### Task selection:

Subtask 2

### Usage:

`python3 <studio song list> <live concert list> <output flie name>`

Example:

`python3 studio_list.txt live_list.txt output.txt`

### Numbers of threads and cores:

single thread, single core

### Expected Memory Footprint:

takes around 1GB disk storage

### Expected Runtime:

around 10 minutes per concert (e.g. 17 studio songs)

### Requirements:

- Linux / macOS

- Python3

- `pip3 install tqdm`

- `pip3 install pydub`

- `pip3 install numpy`

  ​